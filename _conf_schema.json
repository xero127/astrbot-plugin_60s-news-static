{
  "target_groups": {
    "description": "需要推送60s新闻的群组唯一标识符列表",
    "type": "list",
    "hint": "[必填]填写需要接收60s新闻推送的群组唯一标识符，如: 你的平台名称(自己起的):GroupMessage:1350989414, napcat:FriendMessage:123456, telegram:FriendMessage:123456",
    "default": ["这里填你的平台名称:GroupMessage:这里填写你的群号"]
  },
  "push_time": {
    "description": "推送时间(以服务器时区为准)",
    "type": "string",
    "hint": "填写推送的时间，如: 08:00, 12:30, 18:00",
    "default": "08:00"
  },
  "show_text_news": {
    "description": "是否显示文字新闻",
    "type": "bool",
    "hint": "是否显示文字新闻，默认隐藏",
    "default": false
  },
  "use_local_image_draw": {
    "description": "是否使用本地图片绘制",
    "type": "bool",
    "hint": "是否使用本地图片绘制，为否则使用api获取图片",
    "default": false
  },
  "news_api_urls": {
   "description": "60s新闻API的URL列表",
   "type": "list",
   "hint": "用于获取新闻数据的API地址列表(顺序从上至下)",
   "default": [
     "https://60s.viki.moe/v2/60s",
     "https://60s.b23.run/v2/60s",
     "https://60s-api-cf.viki.moe/v2/60s",
     "https://60s-api.114128.xyz/v2/60s",
     "https://60s-api-cf.114128.xyz/v2/60s"
   ]
  },
  "news_static_urls": {
   "description": "60s新闻静态主机",
   "type": "list",
   "hint": "用于获取新闻数据的静态主机地址列表(顺序从上至下)",
   "default": [
     "https://60s-static.viki.moe"
   ]
  },
  "timeout": {
    "description": "连接超时时间",
    "type": "int",
    "hint": " ",
    "default": 30
  }
}
